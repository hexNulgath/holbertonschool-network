#!/usr/bin/env bash
#configures an Ubuntu server

# Remove any existing localhost entry and add the new one
sudo grep -v "localhost" /etc/hosts > "mktemp"
echo "127.0.0.2 localhost" | sudo tee -a "mktemp" > /dev/null

# Replace the original /etc/hosts file
sudo mv "mktemp" /etc/hosts

echo "8.8.8.8   facebook.com" | sudo tee -a /etc/hosts > /dev/null